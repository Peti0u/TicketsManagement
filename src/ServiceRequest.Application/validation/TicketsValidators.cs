using ServiceRequest.Application.Dtos; 

namespace ServiceRequest.Application.validation;

public static class TicketsValidators
{
    // public static (bool IsValid, string Message) ValidateCreate(CreateTicketDto dto)
    // {
    //     if (string.IsNullOrEmpty(dto.Object)) return (false, "Le titre est requis.");
    //     return (true, "");
    // }

    // public static (bool IsValid, string Message) ValidateUpdate(UpdateTicketDto dto)
    // {
    //     if (string.IsNullOrEmpty(dto.Object)) return (false, "Le titre est requis.");
    //     return (true, "");
    // }
}